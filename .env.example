# Copy to .env.local for local development (never commit the real value).
# In AWS Amplify, set this as an environment variable in the app settings.
MEMBERSTACK_TOKEN=your_memberstack_jwt_here

# Optional override; defaults to the Architect beta API.
# ARCHITECT_API_BASE=https://api.beta.architect.new
